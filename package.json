{
  "name": "girssa",
  "version": "0.0.2",
  "description": "GitHub RSS Action",
  "main": "index.js",
  "scripts": {
    "fix": "npx prettier --ignore-path .eslintignore --write .",
    "lint": "npx eslint .",
    "build": "NODE_OPTIONS=--no-experimental-fetch npx ncc --debug build --minify --out dist --source-map --license licenses.txt index.js ",
    "test": "npx jest",
    "coverage": "npx jest --coverage",
    "all": "npm run fix && npm run lint && npm run coverage && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nobe4/girssa.git"
  },
  "keywords": [
    "GitHub",
    "Actions",
    "JavaScript"
  ],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/nobe4/girssa/issues"
  },
  "homepage": "https://github.com/nobe4/girssa#readme",
  "dependencies": {
    "@actions/core": "^1.2.5",
    "@actions/github": "^5.0.3",
    "fast-xml-parser": "^4.0.8"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.34.0",
    "eslint": "^8.0.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-prettier": "^4.0.0",
    "jest": "^28.1.0",
    "prettier": "2.6.2"
  },
  "jest": {
    "coverageThreshold": {
      "global": {
        "branches": 100,
        "functions": 100,
        "lines": 100,
        "statements": 0
      }
    }
  }
}
